# 095 — Project-wide Rust style audit

*Snapshot of every CANON Rust crate against the rules in
[`../repos/tools-documentation/rust/style.md`](../repos/tools-documentation/rust/style.md)
and the naming rule in [`../AGENTS.md`](../AGENTS.md). Three
parallel Explore agents produced first-pass punch lists; this
report folds in a verification pass that caught significant
undercounts in nota-serde-core, signal, and nexus.*

The goal is to make all code style-compliant before resuming M0
implementation work. This report enumerates the findings and
surfaces four design questions that need Li's decision before
fix-up begins.

---

## 1 · Method

- Audited every CANON Rust crate (per
  [`../docs/workspace-manifest.md`](../docs/workspace-manifest.md))
  with code today: nota-serde-core, signal, nota-serde,
  nexus-serde, nexus, sema, criome, nexus-cli, lojix-schema,
  lojix-store. (`lojix` is CANON-MISSING — no code yet.)
- Used three parallel `Explore` agents for breadth, then
  verified by direct grep + read for the largest targets.
- CriomOS-cluster repos (CriomOS, horizon-rs, CriomOS-emacs,
  CriomOS-home) excluded — outside core sema scope.

---

## 2 · Findings overview

| Rule | CANON-code violation count |
|---|---|
| **R2** Wrapped field is private (`pub struct X(pub T)`) | **13** sites across 6 crates |
| **R12** Tests in source files (not in `tests/`) | **4** sites |
| **R1** Methods on types, not free functions | **3 patterns**, ~10 sites |
| **R9** Naming — full words by default | **dozens** of sites in nota-serde-core; scattered elsewhere |
| **R11** One concern per file (file naming) | **2** module-name sites in nota-serde-core |
| Other rules (R3 / R4 / R5 / R6 / R7 / R8 / R10 / R13 / R14) | **NONE** observed |

The bulk of work is concentrated in **nota-serde-core**
(naming) and the **R2 fan-out** across signal / sema /
nexus-serde / lojix-store.

---

## 3 · Per-crate punch list

### 3a · nota-serde-core (1808 LoC — largest concentration)

**R9 — Naming, file/module level** *(rename across the crate)*

- [`src/de.rs`](../repos/nota-serde-core/src/de.rs) (651 LoC) — file
  named after the abbreviation. Module declared as
  [`pub mod de;`](../repos/nota-serde-core/src/lib.rs#L43).
  → rename file → `deserializer.rs`; declaration → `pub mod deserializer;`.
- [`src/ser.rs`](../repos/nota-serde-core/src/ser.rs) (580 LoC) — same pattern.
  → rename file → `serializer.rs`; declaration → `pub mod serializer;`.

**R9 — Naming, identifier level** *(all in `de.rs` / `ser.rs`)*

- Local variables `let mut de = …` ([`de.rs:36`](../repos/nota-serde-core/src/de.rs#L36)),
  `let mut ser = …` ([`ser.rs:53`](../repos/nota-serde-core/src/ser.rs#L53)),
  `let de: serde::de::value::StringDeserializer<…> = …`
  ([`de.rs:314`](../repos/nota-serde-core/src/de.rs#L314)).
  → `deserializer` / `serializer`.
- **Struct fields** named `ser` / `de`:
  `SeqSerializer { ser: &'a mut Serializer }`
  ([`ser.rs:446`](../repos/nota-serde-core/src/ser.rs#L446)),
  `NamedSeqSerializer { ser: … }` ([`ser.rs:479`](../repos/nota-serde-core/src/ser.rs#L479)),
  `StructSerializer { ser: … }` ([`ser.rs:507`](../repos/nota-serde-core/src/ser.rs#L507)),
  `MapSerializer { ser: … }` ([`ser.rs:540`](../repos/nota-serde-core/src/ser.rs#L540)),
  `SeqReader { de: self }` ([`de.rs:363`](../repos/nota-serde-core/src/de.rs#L363)),
  `MapReader { de: self, done: false }` ([`de.rs:383`](../repos/nota-serde-core/src/de.rs#L383)),
  `PositionalArgs { de: self }`
  ([`de.rs:403`](../repos/nota-serde-core/src/de.rs#L403)),
  `AtomicBatchInner { de: self }`
  ([`de.rs:341`](../repos/nota-serde-core/src/de.rs#L341)).
  → `serializer` / `deserializer` field names; touches ~50 use-sites
  inside the impls.
- **Visitor parameters `v: V`**: `deserialize_i8(self, v: V)` and ~14
  similar at [`de.rs:177–224`](../repos/nota-serde-core/src/de.rs#L177).
  → `visitor: V`. The unused-version `_v` ([`de.rs:181`](../repos/nota-serde-core/src/de.rs#L181)
  and similar) → `_visitor`.
- `let mut buf = [0u8; 4]` at
  [`ser.rs:272`](../repos/nota-serde-core/src/ser.rs#L272). → `buffer`.
- `pos: usize` field on `Lexer` at
  [`lexer.rs:76`](../repos/nota-serde-core/src/lexer.rs#L76). → `position`.

**R1 — Methods on types** *(see Question Q1 below)*

- Free functions at the crate root: `from_str`, `from_str_nexus`,
  `from_str_with` ([`de.rs:24,28,32`](../repos/nota-serde-core/src/de.rs#L24)),
  `to_string`, `to_string_nexus`, `to_string_with` (`ser.rs`),
  plus the public helpers `is_pascal_case` / `is_lowercase_identifier`
  ([`lexer.rs`](../repos/nota-serde-core/src/lexer.rs)).
  → see Q1.

**Other rules**: NONE observed (errors via `thiserror`, no
inline `mod tests`, no companion-type smell, no anonymous tuple
returns at boundaries, no authored macros, edition 2024, R5
clean).

---

### 3b · signal (~1000 LoC; recently rewritten)

**R2 — Wrapped field is private**

- [`src/slot.rs:26`](../repos/signal/src/slot.rs#L26) — `Slot(pub u64)`.
- [`src/slot.rs:36`](../repos/signal/src/slot.rs#L36) — `Revision(pub u64)`.
- [`src/auth.rs:33`](../repos/signal/src/auth.rs#L33) — `BlsG1(pub [u8; 48])`.

→ Make field private; add `pub fn new(value: …) -> Self` +
either `pub fn value(&self) -> …` or `impl AsRef<…>` (and any
needed `From` / `TryFrom`). `#[serde(transparent)]` does **not**
require a public field — derive macros expand inside the
declaring module and have access to private fields.

**R12 — Tests in source files**

- [`src/frame.rs:67–331`](../repos/signal/src/frame.rs#L67) — large
  inline `#[cfg(test)] mod tests` block.
  → move to `tests/frame.rs`. Public types at the crate root
  reachable; if any tests touch private internals, hoist a
  small `pub(crate)` accessor or relocate tests deliberately.

**R9 — Naming, identifier level** *(scattered)*

- `pub op: Box<BatchOp>` field at
  [`request.rs:46`](../repos/signal/src/request.rs#L46) — bare `op`.
  → `operation` or `batch_op`.
- Doc-comment uses "op" as a standalone noun
  ([`diagnostic.rs:46`](../repos/signal/src/diagnostic.rs#L46),
  [`edit.rs:6,81`](../repos/signal/src/edit.rs#L6)). → "operation"
  in prose.

**Type-name suffix `…Op`** — `AssertOp` / `MutateOp` /
`RetractOp` / `QueryOp` / `BatchOp` — see **Q2** below.

**Other rules**: NONE observed beyond the above.

---

### 3c · nota-serde (29 LoC)

NONE observed — pure façade re-exporting nota-serde-core.

---

### 3d · nexus-serde (93 LoC)

**R2 — Wrapped field is private** — six sentinel wrappers at
[`src/lib.rs:63,69,75,81,87,94`](../repos/nexus-serde/src/lib.rs#L63):
`Bind(pub String)`, `Mutate<T>(pub T)`, `Negate<T>(pub T)`,
`Validate<T>(pub T)`, `Subscribe<T>(pub T)`, `AtomicBatch<T>(pub T)`.
→ Make fields private; add `pub fn new(t: T) -> Self` +
`pub fn into_inner(self) -> T` (or `impl From<T>` /
`impl Deref`).

**Other rules**: NONE observed.

---

### 3e · nexus (~280 LoC)

**R1 — Methods on types**

- [`src/parse.rs:231`](../repos/nexus/src/parse.rs#L231) —
  `fn check_bind_name(got: &str, expected_field_name: &str) -> Result<()>`
  is a module-level free fn. Either fold into `QueryParser`
  as `fn check_bind_name(&self, got: &str, expected_field_name:
  &str) -> Result<()>`, or treat as the "small private helper
  in one module" carve-out. **Recommendation**: fold into
  `QueryParser` since it's specific to query parsing and may
  want access to parser context later (line/column for richer
  diagnostics).

**Other rules**: NONE observed (`QueryParser` itself, error
enum, module layout, tests-in-`tests/` all clean).

---

### 3f · sema (~205 LoC)

**R2 — Wrapped field is private**

- [`src/lib.rs:27`](../repos/sema/src/lib.rs#L27) — `Slot(pub u64)`.
  Same fix pattern as signal's `Slot`. **Note**: sema defines
  its OWN `Slot` (not signal's, per the doc comment at
  [`lib.rs:24`](../repos/sema/src/lib.rs#L24)) — fix is local.

**R12 — Tests in source files**

- [`src/lib.rs:109–205`](../repos/sema/src/lib.rs#L109) — inline
  `#[cfg(test)] mod tests` block. → move to `tests/sema.rs`.

**Other rules**: NONE observed.

---

### 3g · criome (~233 LoC, mostly stubs)

NONE observed. Modules use full English (`error`, `uds`,
`validator`); error enum properly typed; no inline tests; no
free fns beyond `main`; module structure compliant. (`uds`
falls under exception 4: general-English acronym.)

---

### 3h · nexus-cli (~21 LoC stub)

NONE observed.

---

### 3i · lojix-schema (~112 LoC scaffold)

NONE observed.

---

### 3j · lojix-store

**R2 — Wrapped field is private**

- [`src/layout.rs:26`](../repos/lojix-store/src/layout.rs#L26) —
  `StoreRoot(pub PathBuf)`.
- [`src/layout.rs:63`](../repos/lojix-store/src/layout.rs#L63) —
  `StorePath(pub PathBuf)`.
- [`src/hash.rs:14`](../repos/lojix-store/src/hash.rs#L14) —
  `StoreEntryHash(pub [u8; HASH_LEN])`.

**R12 — Tests in source files**

- [`src/layout.rs`](../repos/lojix-store/src/layout.rs) and
  [`src/hash.rs`](../repos/lojix-store/src/hash.rs) — both have
  inline `#[cfg(test)] mod tests` blocks.

**Note**: `lojix-store` is scaffold for a daemon that hasn't
landed. Status is CANON per manifest but it's not on the M0
critical path. Style fixes here are still in scope but lower
priority than signal / sema / nota-serde-core.

---

### 3k · Doc fix

- [`nota/README.md:198`](../repos/nota/README.md#L198) — example
  shows `pub struct Slot(pub u64);`. Update example to private
  field + `new` / accessor pattern so the doc teaches the
  current rule.

---

## 4 · Design questions for Li

These four decisions block clean execution of the punch list.

### Q1 — Crate-level `from_str` / `to_string` free functions

nota-serde-core exposes free functions
[`from_str`](../repos/nota-serde-core/src/de.rs#L24),
[`from_str_nexus`](../repos/nota-serde-core/src/de.rs#L28),
[`from_str_with`](../repos/nota-serde-core/src/de.rs#L32),
`to_string`, `to_string_nexus`, `to_string_with`.

The rule says "behavior lives on types." But this signature
mirrors `serde_json::from_str` / `serde_json::to_string` — a
universally-recognised serde-ecosystem convention. Two options:

**(a)** Treat this as **exception 5** (names inherited from
well-known crates) — keep the free fns; only rename the
modules and locals. Keeps API consonant with serde_json /
serde_yaml / etc.

**(b)** Apply the rule strictly — move logic to
`Deserializer::from_str(input).into_value()` or similar; let
free fns die. Pro: maximally rule-consistent. Con: breaks
ergonomic alignment with the rest of the serde ecosystem.

The same question applies to `is_pascal_case` /
`is_lowercase_identifier` in `lexer.rs` — these are predicate
helpers, candidates for either methods on `Token` or methods on
a `Identifier` newtype.

**My read**: (a) for `from_str`/`to_string` (true serde
convention; users expect this shape), and apply the rule for
the predicate helpers (move to `Token::is_pascal_case` /
similar). But this is a judgment call worth your sign-off.

### Q2 — `…Op` type-name suffix across signal

`AssertOp` / `MutateOp` / `RetractOp` / `QueryOp` / `BatchOp`
all use the `Op` suffix. The bad→good naming table reads:

> `op` → `operation` (or specific: `assert_op`)

I read "(or specific: `assert_op`)" as: `AssertOp` is the
"specific" form and is acceptable. The compound disambiguates
what would otherwise be a bare `op`.

Alternative: rename to `AssertOperation` etc. Feels needlessly
verbose for type names that read naturally as `AssertOp` /
`MutateOp`.

**My read**: keep `…Op` suffix on type names; rename only the
bare-`op` field at [`request.rs:46`](../repos/signal/src/request.rs#L46)
and the prose-doc `op` references. Confirm.

### Q3 — Module-name renames `de.rs` / `ser.rs` → `deserializer.rs` / `serializer.rs`

This rename cascades to every `use crate::ser::…` /
`use crate::de::…` import inside the crate (~30 sites), plus
the `pub mod de` / `pub mod ser` declarations in `lib.rs`.

Mechanically straightforward but produces a wall of `use`
changes in the diff. Worth doing — file/module names are
public surface. **My read**: do it; the cost is one-shot, the
benefit is permanent.

### Q4 — `Slot` / `Revision` accessor shape

When making the field private, three constructor/accessor
shapes are reasonable:

**(a)** `pub fn new(value: u64) -> Self` + `pub fn value(&self) -> u64`.
Symmetric, clear, no trait machinery.

**(b)** `impl From<u64> for Slot` + `impl From<Slot> for u64`.
Idiomatic Rust; allows `slot.into()`.

**(c)** Both — `new` + `value` for explicit call sites,
`From`/`Into` for ergonomic conversion. Slight redundancy.

**My read**: (a) for clarity; add `From<u64>` only if a
specific call site benefits.

---

## 4a · Decisions on Q1–Q4 (Li, 2026-04-27)

### Q1 — Serde: keep it; fix the API surface

**Decision: keep serde.** The serde traits themselves
(`Serializer::serialize_X`, `Deserializer::deserialize_X`,
`Visitor::visit_X`, `SerializeStruct::serialize_field`) are
*already* methods-on-types correctly applied. The
methods-on-types rule is satisfied by serde's design.

What was incorrect was our own crate-level free-function
*shims* (`from_str`, `to_string`, `from_str_nexus`, etc.). Two
options for these:

**(a) Treat as exception 5 (well-known-libraries naming).**
`serde_json::from_str` is the universal shape; users coming
from any serde-format crate expect it. Keep the free-fn shape
at the crate root *only* as the user-facing convenience API,
with a deliberate one-line acknowledgement that this is the
serde-ecosystem convention.

**(b) Strict rule.** Move the logic to
`Deserializer::with_dialect(input).deserialize::<T>()` etc.
Adds 5–10 characters to every call site. Loses the
serde_json-shaped ergonomics.

**Resolution: (a).** The convention is real and load-bearing
across the Rust serde ecosystem. The narrow carve-out is
honest. *Inside* the crate (helper variables, struct fields,
parameters), the methods-on-types rule applies normally — no
local `let mut de = …`, no `SeqSerializer { ser: … }` field
naming.

The deeper "should we use serde at all?" question is **no, do
not replace it**. Building our own ser/de framework would mean
either authoring derive macros (we have a no-macros rule) or
hand-writing ser/de for every record type (impractical at our
record count). Serde's trait surface is good; the issue was
only our top-level convenience layer.

### Q2 — `…Op` type-name suffix: rename to `…Operation`

**Decision: rename.** `AssertOp` / `MutateOp` / `RetractOp` /
`QueryOp` / `BatchOp` become `AssertOperation` /
`MutateOperation` / `RetractOperation` / `QueryOperation` /
`BatchOperation`.

Rationale per Li (2026-04-27): *"Then you'll have to change
how you think. You were taught wrong."* The
`AssertOp`-feels-fine reflex is training-data drift, not
informed judgment. The full English form is what reads as
English. The abbreviated form is ceremony to be decoded.

Backing in the strengthened naming docs:
[`mentci/AGENTS.md` §"feels too verbose" anti-pattern](../AGENTS.md)
and [`beauty-research.md`](../repos/tools-documentation/programming/beauty-research.md).

This affects:

- `signal/src/edit.rs` — `AssertOp`, `MutateOp`, `RetractOp`,
  `BatchOp`.
- `signal/src/query.rs` — `QueryOp`.
- `signal/src/request.rs:46` — the field `pub op: Box<BatchOp>`
  becomes `pub operation: Box<BatchOperation>`.
- All call sites across `signal`, `nexus`, prospective `criome`.
- Doc comments using "op" → "operation."

Estimated diff: ~80 line touches across 8 files.

### Q3 — Module rename `de.rs`/`ser.rs` → `deserializer.rs`/`serializer.rs`

**Decision: rename.** Per Li (2026-04-27): *"See my point above.
Fuck ugliness and non-conciseness."* File names are public
surface; module declarations are public surface; uglier doesn't
mean shorter, it means harder to read.

This affects:

- `nota-serde-core/src/de.rs` → `deserializer.rs`.
- `nota-serde-core/src/ser.rs` → `serializer.rs`.
- `nota-serde-core/src/lib.rs` declarations: `pub mod de;` →
  `pub mod deserializer;` (and `ser` → `serializer`).
- Internal `use crate::ser::…` / `use crate::de::…` imports
  (~30 sites): `use crate::serializer::…` / `use crate::deserializer::…`.
- Local variable names `let mut de = …` / `let mut ser = …` →
  `let mut deserializer = …` / `let mut serializer = …`.
- Struct field names `ser: &mut Serializer` /
  `de: &mut Deserializer` → `serializer:` / `deserializer:`.
- Visitor parameter names `(self, v: V)` → `(self, visitor: V)`.

Estimated diff: ~150 line touches in `nota-serde-core` (the
bulk of the rename work).

### Q4 — `Slot` / `Revision` accessor shape: traits + file splits

**Decision: use `From` traits, not inherent `new`/`value`
methods.** Per Li (2026-04-27): *"Use traits. Don't be afraid
of explicit code; just add a guidance to break up the code into
more files (like splitting up the traits), then we get code
that is just as maintainable while being more correct."*

For `Slot` / `Revision` / `BlsG1` / each
`nexus-serde::{Bind,Mutate,Negate,Validate,Subscribe,AtomicBatch}`:

```rust
// Field is private:
pub struct Slot(u64);

// Construction via trait:
impl From<u64> for Slot { fn from(value: u64) -> Self { Self(value) } }

// Read-out via trait:
impl From<Slot> for u64 { fn from(slot: Slot) -> u64 { slot.0 } }

// Optional borrow accessor (trait or inherent — see below):
impl AsRef<u64> for Slot { fn as_ref(&self) -> &u64 { &self.0 } }
```

Traits over inherent methods because traits compose with the
ecosystem (`.into()` works, generic functions accept `Into<u64>`,
etc.). The rule from Q4: when traits accumulate on a type, split
each trait impl into its own file (per the new
[rust/style.md §"Split traits into their own files"](../repos/tools-documentation/rust/style.md)
guidance).

For the wrapper types `Mutate<T>` / `Negate<T>` / etc., the same
shape: `From<T>` for construction; `From<Wrapper<T>>` for `T` for
read-out; if there is genuine downstream need to access the
inner ergonomically, add `Deref` (single-target type) — but only
if a real call site asks for it.

Estimated diff: ~10 newtype definitions changed + new trait impls
+ ~20 call sites updated to `.into()` (the read-out pattern).

---

## 5 · Proposed order of fixes

Once Q1–Q4 are answered, the natural order is:

1. **R2 fan-out** (slot/revision/wrapper privacy) — touches
   signal, sema, nexus-serde, lojix-store. Mechanical;
   downstream code uses `.value()` instead of `.0` (count
   call sites first; likely <30 across all crates). Plus the
   nota README doc fix.
2. **R12 tests-to-`tests/`** — signal/frame.rs, sema/lib.rs,
   lojix-store/layout.rs + hash.rs. Mechanical move; verify
   tests still see the right surface.
3. **nexus parse.rs free fn → method** — small, isolated.
4. **R9 nota-serde-core large rename pass** — files →
   `deserializer.rs`/`serializer.rs`, locals/fields/params →
   full words. This is the bulk of the work (~50–100 use-site
   touches across 1200 LoC). Land as one self-contained
   commit per file, run `cargo test` between.
5. **R9 signal naming touch-ups** — `op` field rename + prose
   tweaks.

Total estimated diff: ~150 line-touches across ~10 files. No
behavior changes; tests remain green throughout.

When the punch list is resolved, restart from
[`094 §10`](094-handoff-explicit.md) (M0 step 3 — criome body).

---

*End 095.*
